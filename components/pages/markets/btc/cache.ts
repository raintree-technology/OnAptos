// Simple cache for formatting results (browser API is efficient enough)
// Note: Custom LRU cache removed per best practices - browser's built-in
// mechanisms and React's memoization are sufficient for this use case
