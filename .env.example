# ===========================================
# SETUP INSTRUCTIONS
# ===========================================
# 1. Copy this file to .env or .env.local
# 2. Fill in the REQUIRED values marked below
# 3. Optionally customize OPTIONAL values
# 4. Never commit .env to git (already in .gitignore)

# ===========================================
# SITE CONFIGURATION
# ===========================================
# Public-facing site configuration (NEXT_PUBLIC_ vars are exposed to browser)

# REQUIRED: Your site URL (used for SEO, redirects, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OPTIONAL: Customize site branding
NEXT_PUBLIC_SITE_NAME=On Aptos
NEXT_PUBLIC_SITE_DESCRIPTION=Aptos blockchain analytics and portfolio tracking

# OPTIONAL: CORS allowed origins (comma-separated)
NEXT_PUBLIC_CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# OPTIONAL: Aptos Indexer URL (defaults to public endpoint)
NEXT_PUBLIC_APTOS_INDEXER_URL=https://api.mainnet.aptoslabs.com/v1/graphql

# ===========================================
# DEVELOPER / ATTRIBUTION (OPTIONAL)
# ===========================================
# These appear in SEO metadata, footer, etc.
# Leave empty to use defaults

NEXT_PUBLIC_DEVELOPER_NAME=
NEXT_PUBLIC_DEVELOPER_EMAIL=
NEXT_PUBLIC_DEVELOPER_WEBSITE=
NEXT_PUBLIC_DEVELOPER_TWITTER=
NEXT_PUBLIC_DEVELOPER_TWITTER_HANDLE=
NEXT_PUBLIC_DEVELOPER_GITHUB=
NEXT_PUBLIC_DEVELOPER_LINKEDIN=

# ===========================================
# API KEYS (REQUIRED)
# ===========================================

# CoinMarketCap API - REQUIRED for price data
# Get your key at: https://coinmarketcap.com/api/
CMC_API_KEY=
CMC_API_BASE_URL=https://pro-api.coinmarketcap.com/v2

# Aptos Indexer - OPTIONAL (for better rate limits)
# Get your key at: https://developers.aptoslabs.com/
APTOS_BUILD_SECRET=

# RWA (Real World Assets) API - REQUIRED for RWA data
# Contact RWA.xyz for API access
RWA_API_KEY=

# Panora API - OPTIONAL (public key is used by default)
# Leave empty to use public key
# PANORA_API_KEY=

# ===========================================
# DATABASE (OPTIONAL)
# ===========================================
# Only needed if using database features
# DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# ===========================================
# ENVIRONMENT / LOGGING (OPTIONAL)
# ===========================================
NODE_ENV=development
LOG_LEVEL=info
